// Basic audio utilities - simplified version
export const registerAudioBuffer = (id: string, audioBuffer: any): void => {
  // Implementation will be added with full audio system
};

export const getAudioBuffer = (id: string): any => {
  // Implementation will be added with full audio system
  return null;
};
